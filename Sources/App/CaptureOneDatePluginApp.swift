import SwiftUI

@main
struct CaptureOneDatePluginApp: App {
    var body: some Scene {
        WindowGroup {
            Text("Capture One Date Plugin")
                .frame(minWidth: 600, minHeight: 400)
        }
    }
}
